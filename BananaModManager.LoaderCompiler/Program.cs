using System;

namespace BananaModManager.LoaderCompiler
{
    internal class Program
    {
        public static void Main(string[] args)
        {
            Console.WriteLine("You're not actually supposed to run this! This is a really ugly hack to get the loader libraries to compile. Those libraries will be used with the modded game!");
        }
    }
}
